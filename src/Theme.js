import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#1976d2",
      light: "#fff",
    },
    secondary: {
      main: "#fff",
    },
    customColors: {
      lightBlue: "#7af3ff",
    },
  },
});
